import WorkbenchClient from "@/modules/workbench/WorkbenchClient";

export const metadata = {
  title: "Workbench | CRM OS"
};

export default function WorkbenchPage() {
  return <WorkbenchClient />;
}
