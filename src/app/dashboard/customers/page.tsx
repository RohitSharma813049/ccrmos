import CustomersClient from "@/modules/customers/components/CustomersClient";

export const metadata = {
  title: "Customers | CCRM"
};

export default function CustomersPage() {
  return <CustomersClient />;
}
