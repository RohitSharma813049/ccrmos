import FormBuilderClient from "@/modules/settings/components/FormBuilderClient";

export const metadata = {
  title: "Form Builder | Dashboard",
};

export default function FormBuilderPage() {
  return <FormBuilderClient />;
}
