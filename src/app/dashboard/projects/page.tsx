import ProjectsClient from "@/modules/projects/components/ProjectsClient";

export const metadata = {
  title: "Projects | CCRM"
};

export default function ProjectsPage() {
  return <ProjectsClient />;
}
